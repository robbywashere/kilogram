/*
process.env.NODE_ENV = 'test'; // TODO ?
const { Job } = require('../objects');

const sinon = require('sinon');
const assert = require('assert');
const sync = require('../db/sync');
const JobRunner = require('../jobs');
beforeEach(async ()=> {
  return sync(true);
});

describe('android.runner', function(){


})*/
